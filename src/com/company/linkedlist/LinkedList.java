package com.company.linkedlist;

public class LinkedList {

    public Node head;

    public LinkedList() {

    }

    public Node getHead() {
        return null;
    }

    public Node getIndex(Integer index) {
        return null;
    }

    // Loop through every node and check node.getNext().
    // If getNext() is null, it is the last node in the list.
    public Node getTail() {
        return null;
    }
}
